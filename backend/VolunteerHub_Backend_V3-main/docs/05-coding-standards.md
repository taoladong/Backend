# Coding Standards

- Use C# with .NET 8
- Use PascalCase for types and methods
- Use camelCase for locals and parameters
- Use one class per file
- Keep methods focused and short
- Use DI for all services
- Use Result<T> or a consistent service response wrapper
- Validate all user input
- Prefer composition over large god classes
- Add unit tests for service logic